//max number
var num1 = Number(prompt("plz enter the first number: "));
var num2 = Number(prompt("plz enter the secound number: "));
var max = Math.max(num1, num2);
if (max == num1) {
  alert("the bigger number is: " + max);
} else {
  alert("the bigger number is: " + max);
}
