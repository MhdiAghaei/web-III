var squareLine = +prompt("plz enter line:");
function mohit(n) {
  return n * 4;
}
function area(n) {
  return n * n;
}
alert(`mohit dayere: ${mohit(squareLine)}`);
alert(`masahat dayere: ${area(squareLine)}`);
